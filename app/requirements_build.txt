# PPT审查工具 - 打包专用依赖文件
# 用于确保所有必要的包都被正确安装

# 核心依赖
PyYAML>=6.0
python-pptx>=0.6.21
Pillow>=9.0.0
lxml>=4.6.0

# 打包工具
pyinstaller>=5.0

# 可选依赖（如果使用其他LLM服务）
requests>=2.25.0
urllib3>=1.26.0

# 开发依赖（可选）
# pytest>=6.0.0
# black>=22.0.0
# flake8>=4.0.0
